<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ECGWidgetsViewer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:colorControlNormal">@color/purple_200</item> <!-- Change your_color to the desired color -->
        <item name="android:colorControlActivated">@color/grey_light</item> <!-- Change your_color to the desired color -->
        <item name="colorPrimary">@color/color_primary</item>
    </style>

    <style name="Theme.ECGWidgetsViewer" parent="Base.Theme.ECGWidgetsViewer" />
</resources>